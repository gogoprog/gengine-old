#!/bin/bash

BIN_DIR=$(dirname $(readlink -e $0))
BASEDIR=${BIN_DIR%/*}

export PATH=${PATH}:${BASEDIR}/bin
export GENGINE=${BASEDIR}

PROMPT="[gengine]${PROMPT}"

echo "[gengine] Enabled with ${GENGINE}"
